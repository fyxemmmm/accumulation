apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: etcd
  name: etcd2-pv-claim
  namespace: apiservice
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
